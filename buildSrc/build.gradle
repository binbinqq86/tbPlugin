apply plugin: 'groovy'
apply from: "../version.gradle"

repositories {
    jcenter()
    google()
}

dependencies {
    compile gradleApi()
    compile localGroovy()
    compile "com.android.tools.build:gradle:$androidGradleVersion"
}
